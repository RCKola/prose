diff --git a/geotransformer/extensions/common/torch_helper.h b/geotransformer/extensions/common/torch_helper.h
index ad0df3f..cebb2b9 100644
--- a/geotransformer/extensions/common/torch_helper.h
+++ b/geotransformer/extensions/common/torch_helper.h
@@ -1,6 +1,6 @@
 #pragma once
 
-#include <ATen/cuda/CUDAContext.h>
+// Patched: removed <ATen/cuda/CUDAContext.h> to allow CPU-only build.
 #include <torch/extension.h>
 
 #define CHECK_CUDA(x)                                                         \
diff --git a/geotransformer/modules/kpconv/kernel_points.py b/geotransformer/modules/kpconv/kernel_points.py
index 3255979..8946794 100644
--- a/geotransformer/modules/kpconv/kernel_points.py
+++ b/geotransformer/modules/kpconv/kernel_points.py
@@ -14,13 +14,17 @@
 #      Hugues THOMAS - 11/06/2018
 #
 
+import os
 import os.path as osp
 from os import makedirs
 from os.path import join, exists
 
 import matplotlib.pyplot as plt
 import numpy as np
-import open3d as o3d
+try:
+    import open3d as o3d  # optional: only used for cached kernel points on disk
+except ImportError:
+    o3d = None
 
 
 # ------------------------------------------------------------------------------------------
@@ -416,12 +420,30 @@ def load_kernels(radius, num_kpoints, dimension, fixed, lloyd=False):
             # Save points
             kernel_points = kernel_points[best_k, :, :]
 
-        pcd = o3d.geometry.PointCloud()
-        pcd.points = o3d.utility.Vector3dVector(kernel_points)
-        o3d.io.write_point_cloud(kernel_file, pcd)
+        # Cache kernel to .npy (avoids open3d dependency).
+        np.save(kernel_file + ".npy", kernel_points.astype(np.float32))
     else:
-        pcd = o3d.io.read_point_cloud(kernel_file)
-        kernel_points = np.array(pcd.points).astype(np.float32)
+        npy_cache = kernel_file + ".npy"
+        if os.path.exists(npy_cache):
+            kernel_points = np.load(npy_cache).astype(np.float32)
+        elif o3d is not None:
+            pcd = o3d.io.read_point_cloud(kernel_file)
+            kernel_points = np.array(pcd.points).astype(np.float32)
+        else:
+            # Fallback: read PLY via plyfile instead of open3d.
+            try:
+                from plyfile import PlyData  # type: ignore
+                ply = PlyData.read(kernel_file)
+                v = ply["vertex"]
+                kernel_points = np.stack(
+                    [np.asarray(v["x"]), np.asarray(v["y"]), np.asarray(v["z"])],
+                    axis=1,
+                ).astype(np.float32)
+            except Exception as e:
+                raise RuntimeError(
+                    f"Kernel file {kernel_file} exists but cannot be read "
+                    f"(open3d unavailable, plyfile fallback failed: {e})."
+                )
 
     # Random roations for the kernel
     # N.B. 4D random rotations not supported yet
diff --git a/geotransformer/modules/registration/procrustes.py b/geotransformer/modules/registration/procrustes.py
index 7a53892..448bb20 100644
--- a/geotransformer/modules/registration/procrustes.py
+++ b/geotransformer/modules/registration/procrustes.py
@@ -1,4 +1,3 @@
 import torch
 import torch.nn as nn
-import ipdb

diff --git a/setup.py b/setup.py
index bfb00b9..7846023 100644
--- a/setup.py
+++ b/setup.py
@@ -1,12 +1,12 @@
 from setuptools import setup
-from torch.utils.cpp_extension import BuildExtension, CUDAExtension
+from torch.utils.cpp_extension import BuildExtension, CppExtension
 
 
 setup(
     name='geotransformer',
     version='1.0.0',
     ext_modules=[
-        CUDAExtension(
+        CppExtension(
             name='geotransformer.ext',
             sources=[
                 'geotransformer/extensions/extra/cloud/cloud.cpp',
